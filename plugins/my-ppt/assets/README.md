# Assets

Use this folder for reusable PPT design material:

- `templates/`: `.pptx` templates or master decks.
- `brands/`: colors, fonts, logos, and example slides by brand.
- `examples/`: reference screenshots or finished deck samples.
- `exports/`: generated previews used while checking layout quality.

Keep large private client material out of git unless the repository is private and intended for it.
